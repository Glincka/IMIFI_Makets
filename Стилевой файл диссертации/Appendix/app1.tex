\appChapter{Test}
Пример того, как выглядит приложение. Окружения имеют букву согласно индексу приложения.
\begin{defin}
    Назовем следующее \dots
\end{defin}
\begin{lstlisting}
    int a = 1, b;
    b = a * 2;    /* неоптимально    */
    b = a << 2;    /* оптимально    */
\end{lstlisting}